#!/usr/bin/env sh

FILE=~/.remote-clipboard
OUT_PORT=33041

if hash xsel 2>/dev/null; then
  xsel -ib
else
  cat > "$FILE"
  chmod 700 "$FILE"
  cat "$FILE" | nc localhost $OUT_PORT 2>/dev/null ||
      echo "Couldn't send to local clipboard" >&2
fi
