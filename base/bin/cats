#!/usr/bin/env bash

set -Eeou pipefail

NOTFIRST=0

for FILE in "$@"; do
  if [[ "$NOTFIRST" != 0 ]]; then
    echo
  fi
  NOTFIRST=1
  echo -e "\u001b[34;1m$FILE"
  for i in $(seq ${#FILE}); do
    printf "="
  done
  echo -e "\u001b[0m"
  cat "$FILE"
done